# Copy to .env and edit. .env is gitignored.
TZ=UTC
RUSTY_DLNA_MEDIA=./media
# If files.db stored a host realpath different from /storage/video,
# bind that path onto itself inside the container:
# RUSTY_DLNA_MEDIA_AT=/path/to/realpath
RUSTY_DLNA_CACHE=./data/cache
# RUSTY_DLNA_CONF=./rusty-dlna.live.toml
# RUST_LOG=info
