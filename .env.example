# Copy to .env and edit. .env is gitignored.
TZ=UTC
RUSTY_DLNA_MEDIA=./media
# If files.db stored the host realpath (symlink resolved), bind it
# onto itself inside the container:
# RUSTY_DLNA_MEDIA_AT=/storage/z2-5x20tb/video
RUSTY_DLNA_CACHE=./data/cache
# RUSTY_DLNA_CONF=./rusty-dlna.live.toml
# RUST_LOG=info
