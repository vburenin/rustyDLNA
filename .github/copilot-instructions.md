# Copilot instructions

Follow **AGENTS.md** at the repository root as the source of truth for decision policy, commands, boundaries, and the verify loop (`scripts/agent-verify.sh`).

Do not duplicate that contract here.
